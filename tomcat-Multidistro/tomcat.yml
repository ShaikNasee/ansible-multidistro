--- 
- name: installing tomcat server on ubuntu
  hosts: appservers
  become: yes
  tasks:
    - name: installing the java 
      ansible.builtin.package:
        name: "{{ java_package_name}}"
        state: present
     
        