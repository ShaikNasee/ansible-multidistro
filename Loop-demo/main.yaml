---
- name: installing utilities softwared on localhost
  hosts: all 
  tasks:
    - name: installing utilities softwares
      ansible.builtin.apt:
        name: "{{ item }}"
      become: yes
        state: present
      loop:
        - git 
        - tree 
        - nano 
        - net-tools
